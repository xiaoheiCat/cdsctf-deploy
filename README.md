# cdsctf-deploy
Deploy CdsCTF by few simple commands. Using Docker + K3s Deploy. Suitable for China users.

## Get Started
```bash
git clone https://github.com/xiaoheiCat/cdsctf-deploy.git cdsctf
cd cdsctf
chmod +x deploy.sh
./deploy.sh
```
